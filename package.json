{
  "name": "quiz-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:backend": "npm install --prefix backend && npm start --prefix backend",
    "build:backend": "npm install --prefix backend && npm run build --prefix backend",
    "build": "concurrently \"npm run build:frontend\" \"npm run build:backend\"",
    "build:frontend": "npm run build --prefix frontend",
    "deploy": "echo 'Nothing to deploy from root, deploy frontend and backend separately'"
  },
  "dependencies": {
    "concurrently": "^9.1.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
